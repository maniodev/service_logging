require 'spec_helper'

describe ServiceLogging do
  it 'has a version number' do
    expect(ServiceLogging::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
